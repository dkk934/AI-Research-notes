## 🔹 Python Basics

### 1️⃣ Variables, Loops, and Functions

**Variables** store data values

```python
x = 10
name = "Python"
```

**Loops** repeat tasks efficiently

* `for` loop
* `while` loop

**Functions** group reusable logic

```python
def greet():
    print("Hello!")
```

---

### 2️⃣ Data Structures

**Lists** – ordered, mutable

```python
nums = [1, 2, 3]
```

**Tuples** – ordered, immutable

```python
point = (10, 20)
```

**Dictionaries** – key-value pairs

```python
student = {"name": "Alex", "age": 20}
```

---

### 3️⃣ File Handling

Used to read/write data from files:

```python
with open("data.txt", "r") as file:
    content = file.read()
```

**Common Modes**:

* `r` → read
* `w` → write (overwrite)
* `a` → append

---

### 4️⃣ Object-Oriented Programming (OOP)

* **Classes** define blueprints
* **Objects** are instances of classes

```python
class Car:
    def __init__(self, brand):
        self.brand = brand

my_car = Car("Tesla")
```

---

## 📌 Why Python?

* Simple & readable syntax
* Huge community support
* Cross-platform
* **90% of AI/ML libraries use Python**, including:

  * TensorFlow
  * PyTorch
  * Scikit-learn
  * NumPy
  * Pandas

---

📘 *These notes are suitable for beginners, revision, slides, or as an introduction to AI/ML learning paths.*
